"""TRL DPOTrainer script — not yet scoped to a phase, needs GPU."""
from __future__ import annotations


def main(config_path: str) -> None:
    raise NotImplementedError("DPO training not yet scoped; needs GPU + accelerate config.")
